# 企畫書與系統吻合度檢查報告

本文件用於期末展示時快速說明：企畫書規劃、現有系統、資料庫設計與可展示亮點之間的對應關係。

## 總結

目前系統與企畫書高度吻合，核心資料表、關聯、查詢與交易流程皆已具體實作。除了基本 CRUD，本系統也補上可加分的資料庫展示點：多表 JOIN、多對多檢索、Wishlist 撮合、訂單狀態機、評價聚合、歷史均價與需求熱度分析。

建議展示時主打：這不是一般購物網站，而是針對偶像周邊交易問題設計的資料庫系統。

## 吻合度矩陣

| 企畫書規劃 | 系統實作 | 吻合度 |
|---|---|---|
| 使用者、團體、成員、商品、商品成員關聯、訂單、願望清單、評價 8 張核心表 | 已建立 8 張核心表，並額外加入 Chat/Message 支援交易溝通 | 完全吻合 |
| 商品可對應多位成員，解決雙人卡/小分隊卡 N:M 問題 | `Product_Member_Rel` 實作商品與成員多對多關聯 | 完全吻合 |
| 可依團體或成員搜尋商品 | `/products/search` 使用 Product、Product_Member_Rel、Member、Group JOIN 查詢 | 完全吻合 |
| 願望清單自動撮合 | `/users/{id}/wishlist_matches` 與前端 Wishlist match 區塊顯示符合條件商品 | 完全吻合 |
| 訂單狀態控管 | 新訂單為 `Pending`，可依序更新為 `Shipped`、`Completed` | 完全吻合 |
| 售出商品不刪除，以狀態保留歷史紀錄 | 購買後 Product 狀態改為 `Sold`，訂單保留商品與價格快照 | 完全吻合 |
| 評價含包裝、對光/影片、速度 | Review 表與評價表單皆有三項分數 | 完全吻合 |
| 信譽分數動態計算 | 評價送出後會重新計算並回寫 `User.SellerReputation` | 完全吻合 |
| 核心 SQL 聚合查詢 | Analytics 頁籤提供市場歷史均價、需求熱度、賣家信譽排行 | 完全吻合 |
| 查詢效能優化 | 已對 Member、Group、Product、Wishlist、Order、Product_Member_Rel 等常用欄位建立索引 | 完全吻合 |

## 展示亮點

### 1. 多對多精確檢索

偶像周邊常有雙人卡或小分隊卡，單一商品可能對應多位成員。系統使用 `Product_Member_Rel` 中介表，讓搜尋任一成員時，都能找到包含該成員的商品。

可展示操作：
1. 首頁搜尋 `Mingyu`
2. 系統回傳同時標記 `Mingyu` 與 `Wonwoo` 的商品
3. 說明這是 N:M 關聯表的價值

### 2. Wishlist 撮合

使用者可設定想找的成員與最高價格，系統會自動比對目前可購買商品。

可展示操作：
1. 進入 Wishlist
2. 新增成員與預算
3. 系統顯示 `Wishlist match`
4. 說明這是企畫書中的「貨找人」

### 3. 訂單狀態機

訂單不是直接完成，而是依序走：

`Pending -> Shipped -> Completed`

可展示操作：
1. 購買商品後到 Orders
2. 依序按 `Mark as shipped`
3. 再按 `Mark as completed`
4. 完成後才可評價賣家

### 4. 偶像圈專用信譽模型

評價不是單一星等，而是三個符合偶像圈交易情境的分數：

- 包裝分數
- 對光/影片確認分數
- 出貨速度分數

送出評價後，系統會使用 AVG 重新計算賣家信譽並回寫 User 表。

### 5. Analytics 資料庫分析

Analytics 頁籤是最適合拿高分的展示點，因為它明確展示資料庫課重點：

- `GROUP BY`
- `AVG`
- `COUNT`
- `MIN`
- `MAX`
- 多表 JOIN

可展示內容：
- 市場歷史均價：用完成訂單計算各成員商品均價
- 成員需求熱度：用 Wishlist 計算熱門需求與平均預算
- 賣家信譽排行：用 Review 聚合三項評價分數

## 建議展示順序

1. 先講痛點：社群平台搜尋混亂、雙人卡不好查、交易狀態難追蹤。
2. 展示 ERD：指出 8 張核心表與 `Product_Member_Rel`。
3. 搜尋商品：展示 N:M 檢索。
4. Wishlist：展示貨找人撮合。
5. Orders：展示訂單狀態機。
6. Review：展示三項信譽分數。
7. Analytics：展示 GROUP BY/AVG/COUNT 的資料庫價值。

## 可用一句話收尾

本系統的特色不是單純做出買賣頁面，而是把偶像周邊交易中「多人商品搜尋、需求撮合、交易狀態、領域化信譽」都轉換成可查詢、可追蹤、可分析的關聯式資料庫設計。
